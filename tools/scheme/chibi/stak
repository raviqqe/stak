#!/bin/sh

set -e

while getopts l: option; do
  case $option in
  l)
    cp $OPTARG ${OPTARG%.scm}.sld
    ;;
  esac
done

shift $(expr $OPTIND - 1)

main_file=$1
shift 1

chibi-scheme -I . $main_file "$@"
