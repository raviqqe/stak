#!/bin/sh

set -e

while getopts l: option; do
  case $option in
  l)
    libraries="$libraries -m $OPTARG"
    ;;
  esac
done

shift $(expr $OPTIND - 1)

main_file=$1
shift 1

chibi-scheme $libraries $main_file "$@"
