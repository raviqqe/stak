[package]
name = "embedded-script"
version = "0.1.16"
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.6.0", path = "../../root", default-features = false, features = [
  "float",
] }

[build-dependencies]
stak-build = { version = "0.1.53", path = "../../build" }

[lints]
workspace = true
