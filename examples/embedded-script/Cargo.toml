[package]
name = "embedded-script"
version = "0.1.0"
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.3.13", path = "../../root" }

[build-dependencies]
stak = { version = "0.3.13", path = "../../root" }

[lints]
workspace = true
