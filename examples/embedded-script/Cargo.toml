[package]
name = "embedded-script"
version = "0.1.10"
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.4.7", path = "../../root" }

[build-dependencies]
stak-build = { version = "0.1.47", path = "../../build" }

[lints]
workspace = true
