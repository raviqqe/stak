[package]
name = "embedded-script"
version = "0.1.1"
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.3.14", path = "../../root" }

[build-dependencies]
stak-build = { version = "0.1.38", path = "../../build" }

[lints]
workspace = true
