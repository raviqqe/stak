[package]
name = "embedded-script"
version = "0.1.12"
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.5.0", path = "../../root" }

[build-dependencies]
stak-build = { version = "0.1.49", path = "../../build" }

[lints]
workspace = true
