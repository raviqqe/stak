[package]
name = "embedded-script"
version = "0.1.2"
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.3.15", path = "../../root" }
stak-program = { version = "0.1.0", path = "../../program" }

[build-dependencies]
stak-build = { version = "0.1.39", path = "../../build" }

[lints]
workspace = true
