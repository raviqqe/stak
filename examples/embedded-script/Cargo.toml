[package]
name = "embedded-script"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
any-fn = "0.6.3"
rand = "0.10.1"
stak = { path = "../../root", version = "0.12.20" }

[build-dependencies]
stak-build = { path = "../../build", version = "0.12.20" }

[lints]
workspace = true
