[package]
name = "embedded-script"
version = "0.1.7"
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.4.4", path = "../../root" }

[build-dependencies]
stak-build = { version = "0.1.44", path = "../../build" }

[lints]
workspace = true
