[package]
name = "embedded-script"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
any-fn = "0.6.3"
rand = "0.10.0"
stak = { path = "../../root", version = "0.12.10" }

[build-dependencies]
stak-build = { path = "../../build", version = "0.12.10" }

[lints]
workspace = true
