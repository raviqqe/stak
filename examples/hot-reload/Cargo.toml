[package]
name = "hot-reload"
version.workspace = true
publish = false
edition.workspace = true

[features]
default = ["hot-reload"]
hot-reload = ["stak/hot-reload"]

[dependencies]
axum = "0.8.1"
stak = { version.workspace = true}
tokio = { version.workspace = true}

[build-dependencies]
stak-build = { version.workspace = true}

[lints]
workspace = true
