[package]
name = "hot-reload"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
axum = "0.8.9"
stak = { path = "../../root", version = "0.12.11" }
tokio = { version = "1.52.2", features = ["rt-multi-thread"] }

[build-dependencies]
stak-build = { path = "../../build", version = "0.12.11" }

[features]
default = ["hot-reload"]
hot-reload = ["stak/hot-reload"]

[lints]
workspace = true
