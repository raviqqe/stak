[package]
name = "custom-vm"
version = "0.1.4"
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.8.1", path = "../../root" }

[build-dependencies]
stak-build = { version = "0.1.58", path = "../../build" }

[lints]
workspace = true
