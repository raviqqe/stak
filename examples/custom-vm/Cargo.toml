[package]
name = "custom-vm"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
stak = { version = "0.10.34", path = "../../root" }

[build-dependencies]
stak-build = { version = "0.10.34", path = "../../build" }

[lints]
workspace = true
