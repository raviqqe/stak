[package]
name = "custom-vm"
version = "0.1.2"
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.7.2", path = "../../root" }

[build-dependencies]
stak-build = { version = "0.1.56", path = "../../build" }

[lints]
workspace = true
