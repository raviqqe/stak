[package]
name = "custom-vm"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
stak = { path = "../../root", version = "0.12.0" }

[build-dependencies]
stak-build = { path = "../../build", version = "0.12.0" }

[lints]
workspace = true
