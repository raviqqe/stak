[package]
name = "custom-vm"
version.workspace = true
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.10.18", path = "../../root" }

[build-dependencies]
stak-build = { version = "0.10.18", path = "../../build" }

[lints]
workspace = true
