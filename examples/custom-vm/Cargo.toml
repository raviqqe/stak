[package]
name = "custom-vm"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
stak = { path = "../../root", version = "0.12.15" }

[build-dependencies]
stak-build = { path = "../../build", version = "0.12.15" }

[lints]
workspace = true
