[package]
name = "custom-vm"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
stak = { path = "../../root", version = "0.11.3" }

[build-dependencies]
stak-build = { path = "../../build", version = "0.11.3" }

[lints]
workspace = true
