[package]
name = "custom-vm"
version = "0.1.1"
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.7.1", path = "../../root" }

[build-dependencies]
stak-build = { version = "0.1.55", path = "../../build" }

[lints]
workspace = true
