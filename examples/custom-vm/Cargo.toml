[package]
name = "custom-vm"
version = "0.1.3"
publish = false
edition.workspace = true

[dependencies]
stak = { version = "0.8.0", path = "../../root" }

[build-dependencies]
stak-build = { version = "0.1.57", path = "../../build" }

[lints]
workspace = true
