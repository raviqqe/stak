[package]
name = "custom-vm"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
stak = { version = "0.11.1", path = "../../root" }

[build-dependencies]
stak-build = { version = "0.11.1", path = "../../build" }

[lints]
workspace = true
