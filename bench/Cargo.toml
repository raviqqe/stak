[package]
name = "stak-bench"
version.workspace = true
edition.workspace = true
publish = false

[[bench]]
harness = false
name = "main"

[[bench]]
harness = false
name = "embed"

[dependencies]
mlua = { version = "0.11.1", features = ["lua54"] }
stak = { version = "0.10.37", path = "../root" }
stak-compiler = { version = "0.10.37", path = "../compiler" }

[dev-dependencies]
criterion = { version = "2.7.2", package = "codspeed-criterion-compat" }

[build-dependencies]
stak-build = { version = "0.10.37", path = "../build" }

[lints]
workspace = true
