[package]
name = "stak-sac"
description = "Stand-Alone Complex for Stak Scheme"
version = "0.1.64"
edition.workspace = true
keywords.workspace = true
license-file.workspace = true
readme.workspace = true
repository.workspace = true

[features]
float = ["stak-vm/float"]
libc = [
  "dep:libc",
  "dep:stak-util",
  "stak-device/libc",
  "stak-file/libc",
  "stak-time/libc",
]
std = [
  "dep:clap",
  "dep:main_error",
  "stak-device/std",
  "stak-file/std",
  "stak-process-context/std",
  "stak-time/std",
]

[dependencies]
clap = { version = "4.5.18", features = ["derive"], optional = true }
libc = { version = "0.2", default-features = false, optional = true }
main_error = { version = "0.1.2", optional = true }
stak-configuration = { version = "0.1.58", path = "../configuration" }
stak-device = { version = "0.2.62", path = "../device" }
stak-file = { version = "0.4.20", path = "../file" }
stak-macro = { version = "0.1.63", path = "../macro" }
stak-process-context = { version = "0.2.20", path = "../process_context", features = [
  "libc",
] }
stak-r7rs = { version = "0.7.2", path = "../r7rs" }
stak-time = { version = "0.1.3", path = "../time" }
stak-util = { version = "0.1.38", path = "../util", optional = true }
stak-vm = { version = "0.5.20", path = "../vm" }
