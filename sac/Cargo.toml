[package]
name = "stak-sac"
description = "Stand-Alone Complex for Stak Scheme"
version = "0.1.48"
edition.workspace = true
keywords.workspace = true
license-file.workspace = true
readme.workspace = true
repository.workspace = true

[features]
libc = ["dep:libc", "dep:stak-util", "stak-device/libc", "stak-file/libc"]
std = [
  "dep:clap",
  "dep:main_error",
  "stak-device/std",
  "stak-file/std",
  "stak-primitive/std",
  "stak-process-context/std",
  "stak-vm/std",
]

[dependencies]
clap = { version = "4.5.16", features = ["derive"], optional = true }
libc = { version = "0.2", default-features = false, optional = true }
main_error = { version = "0.1.2", optional = true }
stak-configuration = { version = "0.1.42", path = "../configuration" }
stak-device = { version = "0.2.46", path = "../device" }
stak-file = { version = "0.4.4", path = "../file" }
stak-macro = { version = "0.1.47", path = "../macro" }
stak-primitive = { version = "0.5.4", path = "../primitive" }
stak-process-context = { version = "0.2.4", path = "../process_context", features = [
  "libc",
] }
stak-util = { version = "0.1.22", path = "../util", optional = true }
stak-vm = { version = "0.5.4", path = "../vm" }
