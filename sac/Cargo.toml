[package]
name = "stak-sac"
description = "Stand-Alone Complex for Stak Scheme"
version = "0.1.59"
edition.workspace = true
keywords.workspace = true
license-file.workspace = true
readme.workspace = true
repository.workspace = true

[features]
float = ["stak-vm/float"]
libc = ["dep:libc", "dep:stak-util", "stak-device/libc", "stak-file/libc"]
std = [
  "dep:clap",
  "dep:main_error",
  "stak-device/std",
  "stak-file/std",
  "stak-process-context/std",
]

[dependencies]
clap = { version = "4.5.17", features = ["derive"], optional = true }
libc = { version = "0.2", default-features = false, optional = true }
main_error = { version = "0.1.2", optional = true }
stak-configuration = { version = "0.1.53", path = "../configuration" }
stak-device = { version = "0.2.57", path = "../device" }
stak-file = { version = "0.4.15", path = "../file" }
stak-macro = { version = "0.1.58", path = "../macro" }
stak-primitive = { version = "0.5.15", path = "../primitive" }
stak-process-context = { version = "0.2.15", path = "../process_context", features = [
  "libc",
] }
stak-util = { version = "0.1.33", path = "../util", optional = true }
stak-vm = { version = "0.5.15", path = "../vm" }
