---
title: Compiler demo
description: Stak Scheme compiler demo running on web browsers.
---

import CompilerDemo from "../../../components/CompilerDemo.astro";

Stak Scheme can run on many platforms including web browsers. The following is the demo of a bytecode compiler for Stak Scheme running on a web page. The bytecode compiler is written in Scheme. In the demo, a virtual machine is running the compiler itself compiled into bytecodes.

<CompilerDemo />
