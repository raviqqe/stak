[package]
name = "stak"
description = "Stak Scheme interpreter"
version = "0.5.2"
edition.workspace = true
keywords.workspace = true
license-file.workspace = true
readme.workspace = true
repository.workspace = true

[features]
default = ["float", "std"]
float = ["stak-sac/float", "stak-vm/float"]
hot-reload = ["stak-macro/hot-reload"]
libc = [
  "stak-device/libc",
  "stak-file/libc",
  "stak-process-context/libc",
  "stak-sac/libc",
  "stak-time/libc",
]
std = [
  "stak-device/std",
  "stak-file/std",
  "stak-process-context/std",
  "stak-sac/std",
  "stak-time/std",
]

[dependencies]
stak-device = { version = "0.2.92", path = "../device" }
stak-file = { version = "0.5.6", path = "../file" }
stak-macro = { version = "0.2.11", path = "../macro" }
stak-module = { version = "0.1.12", path = "../module" }
stak-native = { version = "0.1.6", path = "../native" }
stak-process-context = { version = "0.2.50", path = "../process_context" }
stak-r7rs = { version = "0.9.6", path = "../r7rs" }
stak-sac = { version = "0.1.94", path = "../sac" }
stak-time = { version = "0.1.33", path = "../time" }
stak-vm = { version = "0.7.21", path = "../vm" }

[dev-dependencies]
stak-build = { path = "../build" }

[build-dependencies]
stak-build = { version = "0.1.51", path = "../build" }

[lints]
workspace = true
