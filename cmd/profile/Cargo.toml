[package]
name = "stak-profile"
description = "Stak Scheme profiler"
version = "0.1.15"
edition.workspace = true
keywords.workspace = true
license-file.workspace = true
readme.workspace = true
repository.workspace = true

[features]
float = ["stak-vm/float"]

[dependencies]
clap = { version = "4.5.15", features = ["derive"] }
main_error = "0.1.2"
stak-configuration = { version = "0.1.41", path = "../../configuration" }
stak-device = { version = "0.2.45", path = "../../device", features = ["std"] }
stak-file = { version = "0.4.3", path = "../../file", features = ["std"] }
stak-primitive = { version = "0.5.3", path = "../../primitive", features = [
  "std",
] }
stak-process-context = { version = "0.2.3", path = "../../process_context", features = [
  "std",
] }
stak-profiler = { version = "0.1.14", path = "../../profiler" }
stak-vm = { version = "0.5.3", path = "../../vm", features = [
  "profile",
  "std",
] }

[lints]
workspace = true
