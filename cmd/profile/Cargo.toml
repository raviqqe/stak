[package]
name = "stak-profile"
description = "Stak Scheme profiler"
version = "0.1.14"
edition.workspace = true
keywords.workspace = true
license-file.workspace = true
readme.workspace = true
repository.workspace = true

[features]
float = ["stak-vm/float"]

[dependencies]
clap = { version = "4.5.14", features = ["derive"] }
main_error = "0.1.2"
stak-configuration = { version = "0.1.40", path = "../../configuration" }
stak-device = { version = "0.2.44", path = "../../device", features = ["std"] }
stak-file = { version = "0.4.2", path = "../../file", features = ["std"] }
stak-primitive = { version = "0.5.2", path = "../../primitive", features = [
  "std",
] }
stak-process-context = { version = "0.2.2", path = "../../process_context", features = [
  "std",
] }
stak-profiler = { version = "0.1.13", path = "../../profiler" }
stak-vm = { version = "0.5.2", path = "../../vm", features = [
  "profile",
  "std",
] }

[lints]
workspace = true
