[package]
name = "stak"
version = "0.1.0"
edition = "2021"

[features]
trace = ["vm/trace"]

[dependencies]
device = { package = "stak-device", path = "../device", features = ["std"] }
vm = { package = "stak-vm", path = "../vm", features = ["std"] }
