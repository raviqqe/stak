[package]
name = "stak-vm"
version = "0.1.0"
edition = "2021"

[features]
gc_always = []
std = []
trace = []
trace_heap = []

[dependencies]
cfg-if = "1.0.0"
code = { package = "stak-code", path = "../code" }
device = { package = "stak-device", path = "../device" }

[dev-dependencies]
code = { package = "stak-code", path = "../code", features = ["alloc"] }
insta = "1.31.0"
